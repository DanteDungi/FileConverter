export default function Header({ title }) {
    return (
        <header className="flex justify-cneter font-serif font-bold text-4xl mx-2 py-6">
            <h1>{title}</h1>
        </header>
    );
}
